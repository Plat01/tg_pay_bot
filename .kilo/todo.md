# TODO

## Приоритет платежных провайдеров

**Статус:** не реализовано, заглушка в коде.

Кнопка в боте соответствует типу способа оплаты (СБП, карта РФ, международная
карта, ЕРИП, крипта), а не конкретному провайдеру. Если один и тот же тип
поддерживают несколько провайдеров, нужно выбрать, через кого пойдет платеж.

Сейчас порядок задан заглушкой в
`PaymentMethodsService.get_provider_priority()` (`src/services/payment_methods.py`):
первым идет `DEFAULT_PAYMENT_PROVIDER`, остальные — в порядке регистрации в
`PaymentProviderFactory`.

Что нужно сделать, когда приоритет будет определен:

- [ ] Решить, где хранится приоритет: переменная окружения
      (например, `PAYMENT_PROVIDER_PRIORITY=platega,yookassa`), таблица в БД с
      редактированием через админ-команды, или приоритет отдельно для каждого
      типа способа оплаты
- [ ] Заменить заглушку в `get_provider_priority()` на реальный источник
- [ ] Добавить фолбэк: если выбранный провайдер вернул ошибку при создании
      платежа, пробовать следующего по приоритету
- [ ] Покрыть тестами в `tests/services/test_payment_methods.py`
- [ ] Обновить раздел «Доступные способы оплаты» в README.md
